import { CurrentlyReading } from './currently-reading';

describe('CurrentlyReading', () => {
  it('should create an instance', () => {
    const directive = new CurrentlyReading();
    expect(directive).toBeTruthy();
  });
});
